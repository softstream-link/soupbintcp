[package]
name = "soupbintcp_model"
version.workspace = true
authors.workspace = true
readme.workspace = true
license-file.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true

description = "Data model bindings for soupbintcp exchange protocol"

[dependencies]
byteserde = { workspace = true }
byteserde_derive = { workspace = true }
byteserde_types = { workspace = true }
derive_more = { workspace = true }

# [dev-dependencies]
log = { workspace = true, optional = true }
env_logger = { workspace = true, optional = true }
colored = { workspace = true, optional = true }

[features]
default = []
full = ["unittest"]
unittest = [
    "dep:log",
    "dep:env_logger",
    "dep:colored",
] # these are optional instead of dev-dependencies because unittest package is exported to connect crait 
